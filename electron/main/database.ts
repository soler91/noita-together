import Dexie from "dexie";
/*https://github.com/dexie/Dexie.js
https://dexie.org/docs/Tutorial/Vue
export const db = new Dexie("nt-db");
db.version(1).stores({
  friends: "++id, name, age", // Primary key and indexed props
});
*/
